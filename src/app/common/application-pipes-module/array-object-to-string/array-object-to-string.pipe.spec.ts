import { ArrayObjectToStringPipe } from './array-object-to-string.pipe';

describe('ArrayObjectToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayObjectToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
